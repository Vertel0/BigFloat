#ifndef COMP_H
#define COMP_H
#include <complex>
#include <string>
#include <map>
using namespace std;

string formatComplex(complex<double> value);
string ParseArguments(int argc, char* argv[]);

#endif
